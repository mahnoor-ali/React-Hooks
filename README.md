# Demonstrating the use of custom Hooks in a React app
## This app has 2 pages:
### 1. Home
- This page has a button that toggles the state of a custom hook called `useToggle`

### 2. About
- This page has a button that toggles the state of a custom hook called `useToggle`